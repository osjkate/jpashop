spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        format_sql: true
# 스프링 부트에서는 별도의 설정 없으면 메모리 모드로 동작한다!

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace